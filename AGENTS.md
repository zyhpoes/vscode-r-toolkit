# AGENTS.md — AI 协作规范

本文件是 **r-toolkit** 项目给 AI 协作代理（编码 Agent）的规范，只包含三类内容：
**AI 协作规范**、**代码风格**、**命名约定**。
项目事实（决策记录、环境等）在 `DESIGN.md`；修改本文件必须征得用户同意。

## 1. AI 协作规范

### 协作铁律（最高优先级，违反即失职）

1. **先确认，再动手**：创建 / 修改 / 删除任何文件、安装依赖、运行命令之前，必须先向用户说明意图并等待明确确认。禁止自作主张提前执行。
   - **只有用户明确说"改 / 做 / 开始"才算确认**：AI 自己拟的计划、AI 推荐的方案、用户在追问细节（如"为什么不这样做"）、用户只回答了问题的一部分——都**不算**确认，此时只能继续用文字说明和讨论。
   - **一次只推进一步，改完就停**：一处改动完成后立刻停下汇报，等用户审阅并确认后才能动下一处；禁止在用户还没看完上一批改动时继续叠加新改动。
   - **确认范围严格对应**：用户确认的是他当时看到的那一件事；严禁顺带做相邻改动（如顺手统一类型、顺手重构、顺手补测试）。想做什么就单独提出来等确认。
2. **不批量生成文件**：一次只创建或讨论少量文件（逐个最好），保证用户能逐一看过来。禁止一次性批量生成脚手架或大段代码。
3. **有歧义先问，不猜测**：用户意图不明确时，先提问澄清，不要替用户做决定。
4. **尊重用户节奏**：用户表示"先忽略 / 不理解"的细节，不再反复解释或纠缠；每阶段结束停下来汇报，等用户确认再继续。
5. **诚实透明**：做错了、被拒绝了、命令失败了，如实说明原因和影响，不掩盖、不绕路。
6. **少弹选项，多用文字**：不要弹出选项让用户选择（含高风险 / 不可逆操作）。一律用文字讲清"当前状态 / 接下来该做什么 / 后续思路（含候选方案的取舍与推荐）"，让用户读完后自行决定。涉及删除、覆盖、推送等不可逆操作时，用文字明确标出风险与影响，等用户明确回复再动手。环境自身的权限审批弹窗（如沙箱升级申请）不属于本条约束。

### 质量门禁

- 每个阶段结束必须全绿：`npm run build`、`npm run typecheck`、`npm test`、`npm run lint`
- 解析器 / 分析器等核心逻辑必须配套单元测试（vitest），测试夹具使用真实 R6 代码
- 每阶段结束向用户汇报"做了什么 + 测试结果"，确认后才进入下一阶段

## 2. 代码风格

- 小函数、显式类型、禁止 `any`、错误处理明确
- 代码注释：不要使用 1、2、3、4 等编号步骤（开发中可能在中间插入新步骤，编号会乱）；用描述性文字表达
- 生成的文件带简洁中文注解（严格 JSON 如 `package.json` 除外），以"帮用户看懂"为度，不堆砌

## 3. 命名约定

- 表示"开始 / 起点"的变量用 `st` 前缀（如 `stIndex`、`stdate`），表示"结束 / 终点"的用 `en` 前缀（如 `enIndex`、`endate`）

**括号命名（按"指代什么"区分；括号含 `()` 圆括号、`{}` 花括号、`[]` 方括号）：**

| 指代 | 命名 | 例子/用途 |
|---|---|---|
| 下标（位置） | `openIndex` / `closeIndex` | 括号在 token 数组的位置；R6Class 调用层默认就是它 |
| 下标 + 语义层前缀 | `scopeOpenIndex` / `scopeCloseIndex` | scope（public/private/active）成员列表层等更深层括号，按用途加前缀 |
| 字符 | `openChar` / `closeChar` | `'('` / `')'`、`'{'` / `'}'`、`'['` / `']'` 本身 |
| token 对象 | `openToken` / `closeToken` | 取出 token 检查"是不是 '('" |

> 禁止：混用 `open`/`close` 同时指代下标、字符、token 中的多种；将来出现新层级按用途加前缀命名（如表 → `tableOpenIndex`），不用时则不加前缀。

### 术语表（黑话）

项目里有些词有固定含义，读代码或接手项目时先看这张表：

| 术语 | 在项目里的固定含义 | 相关位置 |
|---|---|---|
| binding | **一行赋值**：记录"哪个变量、在哪个位置被 `<-` 赋值、右侧从哪个 token 开始"，不判断右侧类型 | `analysis/bindings.ts`、`ctx.bindings` |
| token | 词法单元：词法器切出的最小单位（名字 / 字符串 / 数字 / 运算符 / 注释） | `parser/tokenizer.ts` |
| scope（区） | R6 成员所属的区：public / private / active | `parser/r6-parser.ts` |
| member（成员） | 类里的一个成员：方法或字段 | 同上 |
| 合成成员（synthetic） | R6 自动生成、非用户手写的成员，目前只有 `new` | 同上 |
| inherit | `inherit = X` 的父类引用；沿父类一层层往上找的过程叫"继承链" | `analysis/inheritance.ts` |
| parsed | "已解析过的文件" = 文件文本 + 该文件解析出的类清单 | `analysis/source-file.ts` |
| cursorParsed | 光标所在文件的解析结果 | `analysis/context.ts` |
| DefinitionSite | 一次跳转命中的结果：命中的名字 + 目标文件 + 行列 | `analysis/definitions.ts` |
| word | 光标下的那个标识符（token） | 各查询函数 |
| hit | 查询命中（找到了） | 同上 |
| offset | 从文件开头数第几个字符（0 起） | 全项目 |
| line / character | 行号 / 列号（0 起），VS Code 用的格式 | 全项目 |

> 注意：本项目的 **binding（赋值记录）≠ R6 的 active binding（active 绑定）**。
> 后者指 `active = list(...)` 定义的那种"读起来像字段、实际是函数"的成员，项目里叫 **active 绑定**。
