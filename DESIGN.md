# DESIGN.md — 项目清单与进度

> 本文档管两件事：**这个项目要实现什么**（功能清单）+ **做到哪了**（里程碑）。
> 架构、数据结构、实现思路等，等实际做到时再补充。

## 项目是什么

一个 VS Code 插件：让 R 语言的 **R6 面向对象编程** 用起来更顺手（点一下能跳转、敲代码有提示、鼠标悬停有说明、大纲能看到类结构）。

## v1 功能清单（按依赖顺序，带进度）

- [x] **1. 识别 R6 类定义**（地基）✅ 已完成
      插件能认出 `R6Class(...)`，知道类名、成员（方法/字段/绑定/区）、定义位置。
- [x] **2. 文档大纲** ✅ 已实现（暂缓完善）
      大纲面板 / 面包屑 / `Ctrl+Shift+O` 显示树形结构：`类 → 区（public/private/active）→ 成员`，点击跳转。
      已实现：空区不生成节点；区节点位置指向区关键字；v1 只列 R6 类（文件级函数/变量未列）。
      待完善：① 成员范围只覆盖名字（未覆盖整个 `greet = function() {...}`）；
      ② 成员图标不区分方法/字段（需要 parser 给 `R6Member` 加"是不是函数"标记）。
- [x] **3. 定义跳转** ✅ 已完成
      类名跳转 ✅（含跨文件 person$Person）、`self$`/`private$`/`类名$` 成员跳转 ✅、
      实例 `p$xxx` ✅、合成成员 `new` ✅（有 initialize 跳它、否则跳类定义）；
      继承成员跳转 ✅（第 7 项完成后补上：Child$greet / p$xxx / self$ 可跳父类成员，同名遮蔽就近）；
      `super$` ✅（跳父类链同名成员，含跨文件父类）。
- [x] **4. 实例成员跳转（`p$xxx`，L1 同文件）** —— 核心诉求 ✅ 已完成
      `p <- Person$new()` 后 `p$name` 能跳。
      RHS 表达式类型求值器重构已完成（rhs-eval.ts + type-resolve.ts，
      沿"光标前最近一次赋值"追踪 + 别名链递归）：
      `p <- Person`、`p <- person$Person`、`p <- Person$new()`、`p <- q` 均自然识别。
      明确不做：函数参数推断、组合场景（实例作属性，L2）、运行时类型（L3）。
- [ ] **5. 代码补全**
      输入 `self$`、`private$`、`类名$` 后弹成员列表。
- [ ] **6. 悬停信息**
      悬停显示成员说明（如方法参数列表）。
- [x] **7. 继承（inherit）** ✅ 已完成
      parser 提取 `inherit = X`（裸类名 / `pkg$Class` $ 链 / 引号字符串三种写法）；
      继承链解析（父类跨文件查找 + 防环）+ 沿链找成员（最近祖先优先 = 同名遮蔽语义）；
      self$/类名$/实例$ 可跳父类成员；super$ 跳父类链（不含本类）；
      private 不随继承可见（R6 语义，private$ 仍只查本类）。
      范围：父类在已加载文件内（当前 + box 直接依赖一层）。
- [~] **8. 跨文件类索引** 🔄 大部分完成
      已通：当前文件 `box::use` **直接依赖一层**的类名 / 成员 / 继承链跨文件跳转
      （`person$Person`、`p <- Person$new()` 后 `p$greet`、子类里 super$ 跳父类成员 → 均在 person.R）。
      待做：工作区索引 / 递归依赖（祖父类在第三个文件时读不到）；补全、悬停的跨文件成员来源。
- [ ] **9. box 模块支持**（模块跳转 + 模块补全）
      点模块名（`box::use(R/schema/schema)` 里、或 `schema$xxx` 里）→ 跳模块文件；
      点 `schema$func` → 跳模块内定义；`schema$` → 补全模块导出的名字。
      规则见 `docs/项目根解析.md`（box 根怎么算）与 `docs/box-模块规则.md`（模块形态与导出规则）。
      范围：本地模块；顶层符号提取识别 `#' @export` / `box::export()`；`@export box::use(./sub)` 的再导出暂不做。

## 将来可加（现在不做）

- [ ] `.Rmd` 文档里的 R 代码块支持
- [ ] 重命名（F2 批量改名）、查找所有引用
- [ ] `lock_objects = FALSE` 的动态成员
- [ ] 类型注解广泛普及后的增强（R 语言若官方支持类型声明）
- [ ] L2 跨文件实例变量追踪（接受不完美）
- [ ] **全局变量跳转**：点变量名 → 跳它的赋值处（bindings 直接支持，比成员跳转简单）
- [ ] ~~**模块名跳转**：点 `box::use(person)` 或 `person$Person` 里的 person → 跳 person.R 文件顶部~~（已并入第 9 项）
- [ ] 函数跳转（点函数名跳函数定义；识别层已能区分函数/类，跳转待需求）

## 里程碑

| 阶段 | 内容 | 状态 |
|---|---|---|
| S0 方案确认 | 技术路线与步骤表对齐 | ✅ 完成 |
| S1 脚手架 | 配置文件、依赖、空壳扩展可 F5、husky 提交检查 | ✅ 完成 |
| S2 设计文档 | DESIGN.md 定为"功能清单 + 进度" | ✅ 完成 |
| S3 词法器 | R 词法（注释 / 字符串 / 反引号 / 嵌套）+ 单测 | ✅ 完成 |
| S4 解析器 | R6Class 定义解析 + 成员提取 + 单测 | ✅ 完成 |
| S5 分析器 | 引用解析（类名 ✅、成员 ✅、实例变量类型 ✅、继承链 ✅——inherit → 父类查找 + super$，限已加载文件）；跨文件跳转 ✅（box 直接依赖一层）；工作区索引 / 递归依赖 / box.path 解析待做 | 🔄 进行中 |
| S6 Providers | DefinitionProvider ✅（类名 / 变量 / 成员 / 合成成员 new / 继承成员 / super$，含 box 直接依赖的跨文件）；Completion / Hover / DocumentSymbols 待做 | 🔄 进行中 |
| S7 收尾 | 集成测试、性能、vsce 打包、README / CHANGELOG | ⏳ |

## 项目决策记录（已与用户确认，不得擅自更改；更改必须征得用户同意）

| 主题 | 决策 |
|---|---|
| 插件标识 | `name: r-toolkit`，`displayName: R Toolkit` —— 通用命名，定位 R 语言工具集，不只做 R6 |
| 版本号 | SemVer，当前 `0.1.0`（major=0 表示未稳定） |
| 许可证 | `PolyForm-Noncommercial-1.0.0`：源码公开、可学习可修改，**禁止商业用途**；使用标准许可，避免自造条款的法律风险 |
| VS Code 兼容 | `engines.vscode: ^1.135.0`（用户 VS Code 为 1.135.0），`@types/vscode: ~1.134.0`（官方 1.135.0 类型尚未发布，最新为 1.134.0；类型版本 ≤ 引擎版本保证安全，待官方发布后再同步升级） |
| 技术架构 | 纯 TypeScript 扩展，**零运行时依赖**；内置自研解析器（不依赖 tree-sitter 等原生模块） |
| 与 vscode-R 关系 | **分工而非覆盖**：静态智能感知（跳转 / 补全 / 悬停 / 大纲）由本插件承担，用户**无需安装任何 R 包**即可用（vscode-R 的这类能力依赖 `languageserver` + `jsonlite`）；运行时功能（跑代码 / 终端 / 数据查看 / 绘图 / 帮助）交给 vscode-R。`onLanguage:r` 激活，不注册语言。两者同时启用时 VS Code 会**合并**各扩展的 provider（补全可能出现重复项、跳转可能出现多候选、大纲可能两套），本插件通过收窄触发范围与排序减少干扰，但**无法屏蔽对方的 provider**（VS Code 无此 API，也不能程序化禁用别的扩展） |
| 工具链 | TS strict + esbuild 打包 + vitest 测试 + ESLint (typescript-eslint) + @vscode/vsce 打包 |
| 分层原则 | 核心层（tokenizer / parser / analysis）**不 import vscode API**，只输出纯数据（位置用 `{line, character}`）；vscode 类型只在 Provider 边界转换，保证核心逻辑可在纯 Node 中单测 |
| npm 权限方案 | A 方案：项目内 `.npmrc` 配置 `cache=.npm-cache`，避免写用户目录被沙箱拒绝 |

### 已确认的方向决策（重要）

- **实例成员跳转（p$xxx）是本插件核心诉求**（用户原话："做这个插件很大原因就是希望 p$name 能跳转"）
- 变量类型来源优先级：静态推断（`p <- 类$new()`）→ 类型注解（`# @type`）→ ~~交互式确认~~（用户暂不接受）
- 能力边界：L1（同文件）做；L2（跨文件/函数参数）按需；L3（运行时）明确放弃
- **继承范围（已确认）**：父类查找限定在"已加载文件"（当前文件 + box 直接依赖一层）；祖父类在更外层文件（递归依赖）留到工作区索引阶段
- **跨文件方案（已确认）**：目标用户是 box 用户（工程化思想）—— 通过解析 `box::use()` 显式依赖定位要读的文件；不做全工作区盲扫
- **项目根策略（已确认）**：确定 box 根目录按优先级：
  1. `settings.json` 的 `r-toolkit.boxPaths`（**字符串数组**；每个条目支持绝对路径、相对工作区根的相对路径、`${workspaceFolder}` 占位符；配了就只用它）
  2. 向上找最近 `.Rprofile`，解析 `options(box.path = ...)`：
     字面量直读；`'./R'` 等相对路径以该 `.Rprofile` 所在目录为基准；
     `getwd()` / `here()` 等动态值视为该目录
  3. VS Code 工作区根（多根工作区全部作为候选）
  规则细节、推理依据与待定项见 `docs/项目根解析.md`

## 开发环境

- 工作目录：`D:\workspace\vscode-r-toolkit`
- Node v22.19.0 / npm 10.9.3 / git 2.50.0
- 沙箱限制：文件操作仅允许在工作区内；npm 默认缓存目录（用户目录）会被拒绝 → 依赖安装走 `.npmrc` 的 A 方案（缓存指到项目内 `.npm-cache/`）
- 远程仓库：`origin = https://github.com/zyhpoes/vscode-r-toolkit.git`；当前分支 `main`（跟踪 `origin/main`，与本地 `zyh_dev` 同指）；husky + lint-staged 提交时自动跑 eslint + 全项目类型检查
