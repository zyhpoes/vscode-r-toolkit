# DESIGN.md — 功能清单

> 本文档只列"这个项目要实现什么"。架构、数据结构、实现思路等，等实际做到时再补充。
> 清单按"最简单的先做"排序；每完成一项打勾 ✅。

## 项目是什么

一个 VS Code 插件：让 R 语言的 **R6 面向对象编程** 用起来更顺手（点一下能跳转、敲代码有提示、鼠标悬停有说明、大纲能看到类结构）。

## v1 必做清单（按依赖顺序）

- [ ] **1. 识别 R6 类定义**（地基，其它全靠它）
     效果：插件能认出代码里的 `R6Class(...)`，知道"哪个类是哪个、每个类有哪些成员（方法/字段/绑定）、定义在哪个文件哪一行"。
- [ ] **2. 文档大纲**
     效果：编辑器右侧大纲面板出现树形结构：`类 → 方法 / 字段`，点一下跳到对应位置。
- [ ] **3. 定义跳转**
     效果：按住 Ctrl 点击（或按 F12）`self$xxx`、`private$xxx`、`super$xxx`、`类名$xxx`，跳到成员定义的地方；点击类名跳到类定义的地方。
- [ ] **4. 代码补全**
     效果：输入 `self$`、`private$`、`类名$` 后，弹出该上下文可用的成员列表供选择。
- [ ] **5. 悬停信息**
     效果：鼠标悬停在成员上，显示它的说明（例如方法的参数列表）。
- [ ] **6. 继承（inherit）**
     效果：类 A 继承类 B 时，A 里能用 B 的成员；跳转、补全都能认出来。
- [ ] **7. 跨文件**
     效果：一个文件里定义的类，在另一个文件里也能跳转、补全。

## 将来可加（现在不做）

- [ ] `.Rmd` 文档里的 R 代码块支持
- [ ] 重命名（F2 批量改名）、查找所有引用
- [ ] `lock_objects = FALSE` 的动态成员
- [ ] 其它 R 语言能力（S3/S4 类、roxygen 文档等）
